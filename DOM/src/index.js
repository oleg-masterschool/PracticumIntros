// 1- Finding an element on the page and saving a reference to it
//  Older: getElementById, getElementsByTagName, getElementsByClassName
//  Newer: querySelector, querySelectorAll

// Old ways
// getElementById
const nav = null;

// getElementsByClassName
const navItems = null;

// querySelector + querySelectorAll
// A- finding in the entire DOM
const header = null;

const logoName = null;

const firstPlace = null;

// B- finding within a particular element
const imageFirstPlace = null;
const titleFirstPlace = null;
const textFirstPlace = null;

// C- traversing with dot notation
const firstPlaceLinkText = null; // hint => use firstPlace.
const secondParagraphFirstPlace = null; // hint => use textFirstPlace.
const thirdParagraphFirstPlace = null; // hint => use secondParagraphFirstPlace.


// 2- Finding collections of elements in the DOM

// A- Find all the anchor tags inside the nav element
const links = null;

// B- Loop over the links and console.log their text content

// start after this line:
// links

// C- Turn the collection of links into a real array

// start after this line:
// const linksArray
console.log(linksArray);

// D- Use .find / .filter to find the anchor tag with the "textContent" of "Home"

// const foundElement = linksArray.find ...?
// const foundElement = linksArray.filter ...?
console.log(foundElement);

// 3- Changing an element's text content

// A- Change New Hampshire into Tel Aviv

//  B- Research online the difference between "textContent" and "innerText"


// 4- Changing any property

// A- Using dot notation to change a few attributes, like "className", "src"

//  B- Using .setAttribute to change a few attributes


// 5- Changing the styling of an element
// A- By changing the class names on the element with the "classList", for example header

// B- By manipulating inline style attribute on the element


// 6- Creating new elements from scratch and appending them
// Create a new link inside the nav for "Blog"



// 7- Making a copy of the "article" and appending it to the "articles"


